services:
  api:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: sujoydev-api
    restart: unless-stopped
    env_file:
      - ../backend/.env
    environment:
      - NODE_ENV=production
      - PORT=5000
    expose:
      - "5000"
    volumes:
      - api-logs:/app/logs
    networks:
      - sujoydev

  nginx:
    image: nginx:1.27-alpine
    container_name: sujoydev-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/sujoydev.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot-certs:/etc/letsencrypt:ro
    depends_on:
      - api
    networks:
      - sujoydev

volumes:
  api-logs:
  certbot-certs:

networks:
  sujoydev:
    driver: bridge
